{% extends 'base.html.twig' %}

{% block title %} Liste des messages! {% endblock %}


{% block body %}

{% block content %}
<div>
    <h1>
     bienvenu(e) !
     Messagerie de Guinot :
     </h1>

    <br /><br />
    <hr />
<lu>
{#<li><a href="{{ path('expediteur_index') }}"> Le nom de l'expéditeur :</a></li>#}
{#<li><a href="{{ path('destinataire_index', {'id': message.id }) }}"> Le nom du déstinataire :</a></li>#}
{#<li><a href="{{ path('destinataire_index', {'id': message.id }) }}"> Le nom du déstinataire :</a></li>#}
{#<li><a href="{{ path('messageEnvoyer_index', {'id': message.id }) }}">Message envoyer</a></li>#}
{#<li><a href="{{ path('messageRecu_index', {'id': message.id }) }}"> Message reçu :</a></li>#}
{#<li><a href="{{ path('messageLu_index', {'id': message.id }) }}"> Message lu :</a></li>#}
{#<li><a href="{{ path('listeMessagesEnvoyer_index', {'id': message.id }) }}"> Liste des messages envoyer :</a></li>#}
{#<li><a href="{{ path('listeMessagesRecu_index', {'id': message.id }) }}"> Liste des messages reçu :</a></li>#}
</lu>
    
    <br />
    <a href="{{ path('nouveauMessage_index') }}">Nouveau message </a>
<br /><br />
    
    <table border="1">
            <thead>
                <tr>
                    <th> Id </th>
                    <th> Titre du message </th>
                    <th> Description </th>
                    <th> Creer date </th>
                    <th> Si le message est lu </th>
                    <th> Expediteur </th>
                    <th> Destinataire </th>
                    <th> Modifier </th>
<th> Afficher </th>
<th> Supprimer </t  h>
</tr>
            </thead>
            {% for message in message %}
<tbody>
<tr>
                                <td> {{ message.id }} </td>
<td> {{ message.titreMessage }} </td>
                <td> {{ message.description }} </td>
                <td> {{ message.creerDate|date("d/m/Y") }} </td>
<td> {{ message.siMessageLu }} </td>
                <td> {{ message.expediteur.nom }} </td>
                <td> {{ message.destinataire.nom }} </td>
                                <td><a href="{{ path('modifierMessage_index', {'id': message.id }) }}">Modifier</a></td>
<td><a href="{{ path('afficherMessage_index', {'id': message.id }) }}">afficher</a></td>
<td><a href="{{ path('supprimerMessage_index', {'id': message.id }) }}">Supprimer</a></td>
                </tr>
                {% else %}
                <tr>
                <td colspan="5">
                    Vous n'avez pas de messages enregistré dans votre base de données !
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>

</div>
{% endblock %}

<br/><br/>

{% block footer %}
<p>
<a href="{{ path('accueil_index') }}"> Retour à la page d'accueil :</a><br>

</p>
{% endblock %}
{% endblock %}
